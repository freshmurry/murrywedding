<%= form_for @pin, html: { multipart: true } do |f| %>
  <% if @pin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pin.errors.count, "error") %> prohibited this pin from being saved:</h2>

      <ul>
      <% @pin.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <!--<div class="form-group">-->
  <!--  <= f.label :image %><br>-->
  <!--  <= f.file_field :image, class: "btn btn-default form-control" %>-->
  <!--</div>-->
  
  <div class="form-group text-center">
    <span class="btn btn-primary btn-file text-babu">
      <i class="fa fa-cloud-upload" aria-hidden="true"></i> Select A Profile Photo
<!--         <%= f.file_field :image, class: "btn btn-default form-control" %> -->
        <%= file_field_tag "images[]", class: "btn btn-default form-control, type: :file, multiple: true %>
    </span></br>
  </div>

  <div class="form-group">
    <%= f.label :description %><br>
    <%= f.text_field :description, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.submit class: "btn btn-primary" %> 
  </div>
<% end %>
